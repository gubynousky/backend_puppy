spring.application.name=backend_puppy_chop
# BASE DE DATOS
spring.datasource.url=jdbc:postgresql://localhost:5432/puppychop_db
spring.datasource.username=guby
spring.datasource.password=ruby123
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# SERVIDOR
server.port=8080
server.servlet.context-path=/api

# LOGS
logging.level.cl.martinez.backend_puppy_chop=DEBUG

# DATOS INICIALES
spring.sql.init.mode=always
spring.jpa.defer-datasource-initialization=true